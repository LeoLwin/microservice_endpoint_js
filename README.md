# intern_endpoint_js

This is the JavaScript version of the intern_endpoint project.

## Run locally

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — start the server with nodemon
- `npm run start` — run the server directly
- `npm test` — run Vitest tests
# microservice_endpoint_js
